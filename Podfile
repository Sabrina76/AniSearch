# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AniSearch' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AniSearch
  pod 'Parse'

  target 'AniSearchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AniSearchUITests' do
    # Pods for testing
  end

end
